{
  "dependencies": {
    "@tinacms/cli": "^1.5.14",
    "masonry-layout": "^4.2.2",
    "tinacms": "^1.5.6"
  },
  "scripts": {
    "dev": "yarn tinacms build && jekyll build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
